import React, { Component } from "react";

export class KeyBindings extends Component {
  render() {
    return <h6 className="key-bindings">P: Pause | O: Reset</h6>;
  }
}

export default KeyBindings;
